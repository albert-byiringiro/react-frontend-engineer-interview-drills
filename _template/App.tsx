import { useState } from 'react'

// ─── Types ───────────────────────────────────────────────────────────
// Define your data model here. Keep it minimal — only what the spec requires.
// Example:
//   interface Item { id: number; label: string }

// ─── Data / Fixtures ─────────────────────────────────────────────────
// Hardcode sample data here or import from fixture.ts for larger datasets.
// In an interview, start with inline data and refactor only if asked.
// Example:
//   const ITEMS: Item[] = [
//     { id: 1, label: 'First' },
//     { id: 2, label: 'Second' },
//   ]

// ─── Component ───────────────────────────────────────────────────────
function App() {
  // 1. State — what is the minimal state this component needs?
  // 2. Derived values — compute everything you can from state (no extra useState).
  // 3. Handlers — keep event handlers close to the JSX that calls them.

  return (
    <div className="app">
      <h1>Drill Title</h1>
      {/* Build here */}
    </div>
  )
}

export default App
